services:
  socket-server:
    build: .
    container_name: socket-server
    restart: unless-stopped
    ports:
      - "4000:4000"
    environment:
      - NODE_ENV=production
      - PORT=4000
    networks:
      - socket-network

networks:
  socket-network:
    driver: bridge